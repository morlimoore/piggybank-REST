spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/piggybankdb
    username: piggybankuser
    password: password
    driverClassName: org.postgresql.Driver
  jpa:
    generate-ddl: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        ddl-auto: update