spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:5432/${DB_NAME:piggybankdb}
    username: ${SPRING_DATASOURCE_USERNAME:piggybankuser}
    password: ${SPRING_DATASOURCE_PASSWORD:vikkidchamp}
    driverClassName: org.postgresql.Driver
  profiles:
    active: dev

  jpa:
    generate-ddl: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
  mail:
    host: smtp.mailtrap.io
    port: 2525
    username: xxxxx
    password: xxxxx
    protocol: smtp